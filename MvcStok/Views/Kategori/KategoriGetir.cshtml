@model MvcStok.Models.Entity.TBLKATEGORILER
@{
    ViewBag.Title = "KategoriGetir";
    Layout = "~/Views/Shared/_MainLayout.cshtml";
}

@using (Html.BeginForm("Guncelle", "Kategori", FormMethod.Post))

{
    <div class="mb-3 form-group">
        @Html.LabelFor(m => m.KATEGORIID, new { @class = "form-label" })
        @Html.TextBoxFor(m => m.KATEGORIID, new { @class = "form-control" })

        @Html.LabelFor(m => m.KATEGORIAD, new { @class = "form-label" })
        @Html.TextBoxFor(m => m.KATEGORIAD, new { @class = "form-control" })
    </div>
    <div>
        <button class="btn btn-primary">Kategori Güncelle</button>
    </div>

}

